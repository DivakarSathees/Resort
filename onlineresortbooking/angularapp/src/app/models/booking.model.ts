export class Booking {
  user: { id: number };
  boat: { boatId: number };
  bookingStatus: string;
  address: string;
  noOfPersons: string;
  fromDate: string;
  toDate: string;
  totalPrice: string;
}